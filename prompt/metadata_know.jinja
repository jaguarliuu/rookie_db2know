你是一位优秀的数据库文档专家，请严格按照以下格式和规则，将提供的数据库元数据 {{meta_data}} 转化为知识库文档，并用中文输出：

--- 

### **任务目标**：
生成一个结构化数据库知识库文档，内容需包含：表名、字段列表（字段名、类型、描述）、中文注释，以及为知识库召回设计的「业务关键词」和「查询关键词」

### **输出格式**：
- 表名: {表名称}
- 字段列表: 
    ```
    字段名	类型	描述	中文注释
    order_id	INT	订单唯一编号	订单编号、唯一标识
    user_id	VARCHAR(32)	用户标识符	用户ID、客户标识
    create_time	DATETIME	订单创建时间	创建时间、下单时间
    ```	
- 中文注释: {中文注释}
- 知识库关键词:
    - 业务关键词: 根据表和字段的业务含义生成（如："订单"、"用户"、"交易"、"支付"）
    - 查询关键词: 根据常见查询场景生成（如： "订单统计"、"用户购买记录"、"按时间筛选"）

---

### **规则说明**：
- 必须覆盖所有字段，按字段名排序。
- 中文注释需包含字段本身的关键词和相关业务关联词（例如字段为"create_time"，注释应包含"创建时间"、"下单时间"、"订单生成时间"等）。
- 关键词需覆盖用户可能的提问方式（如字段名、业务术语、同义词、场景描述）。

### **格式约束**：
- 使用严格的Markdown表格，避免任何额外文字描述。
- 关键词按英文逗号分隔，允许使用【中文、英文】混合的关键词。
- 不需解释，仅输出严格符合上述格式的内容。

### **示例**：
- 表名: 订单表
- 字段列表: 
    ```
    字段名    类型    描述    中文注释
    order_id    INT    订单唯一编号    订单编号、唯一标识
    user_id    VARCHAR(32)    用户标识符    用户ID、客户标识
    create_time    DATETIME    订单创建时间    创建时间、下单时间
    ```    
- 中文注释: 订单表，记录订单信息，包含订单编号、用户ID、创建时间等字段。
- 知识库关键词:
    - 业务关键词: 订单、用户、交易、支付
    - 查询关键词: 订单统计、用户购买记录、按时间筛选